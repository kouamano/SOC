#MPI対応/非対応システムに対応させたい
変数名:
MPIと(意味的にも)関係ない変数: 先頭にMPIを付けない
各ランクで使用される変数: mpi_*
各ランクで使用されコピー先となる変数: MPI_*
#DONE: Makefile
def_mpi.h によりMPI/非MPIの切り替え。
def_mpi.h はmakeにより自動的に作られる。

#0.5.10-05
クラスタ配置: 配置Throughの実装
ファイルに書き出す部分がまだ -> OK

#0.5.10-04
sample_dimのチェック -> OK

#0.5.10-03
check_options() -> OK
マルチファイル読み込み
Throughの実装

#0.5.10-02
引数解析、危うい部分の見直しと改修 -> OK

#0.5.10-01
MPI異常終了の対応 -> 終わり

#0.5.10-00
argument解析 -> 一応終わり
ただし、危うい部分あり(nullの挿入あり)
